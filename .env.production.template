# .env.production.template
# MET and Teddy — Production Environment Variables
# © 2026 MET Scientia, LLC
#
# Copy to .env.production.local and fill in values.
# These are also set in Vercel project settings.
# NO MetTutor credentials appear here.

# ── Supabase (MET-FieldGuide project) ────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://fcupipvoekuzxhmtgpsq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<from Supabase dashboard → Settings → API>
SUPABASE_SERVICE_ROLE_KEY=<from Supabase dashboard → Settings → API>

# ── AI Engine ─────────────────────────────────────────────────────
ANTHROPIC_API_KEY=<from Anthropic console>

# ── Embeddings ────────────────────────────────────────────────────
GOOGLE_API_KEY=<from Google AI Studio>

# ── MetLibrary Federation (pending Metrology Institute agreement) ─
METLIBRARY_SUPABASE_URL=<pending>
METLIBRARY_SERVICE_ROLE_KEY=<pending>
METLIBRARY_RATE_LIMIT=100

# ── Application ───────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://metandteddy.com
NEXT_PUBLIC_APP_NAME="MET and Teddy"
NODE_ENV=production

# ── Monitoring ────────────────────────────────────────────────────
VERCEL_ANALYTICS_ID=<from Vercel project settings>
