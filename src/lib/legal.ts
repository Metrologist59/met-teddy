// src/lib/legal.ts
// Pinned versions for legal documents accepted during registration.
// The server rejects any acceptance that doesn't match these — a
// stale cached registration page can't record acceptance of a
// document that's since changed.

export const EULA_VERSION = "1.0.0"
export const PRIVACY_VERSION = "1.0.0"
